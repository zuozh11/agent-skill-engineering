---
name: zoom-out
description: 让 agent 跳出当前代码，给出更高层次的全局视角。当你对某段代码不熟悉，或需要理解它在整体中的位置时使用。
---

我对这块代码不熟悉。请上升一层抽象。给我一张所有相关模块和调用者的地图，使用当前任务已经由 `project-knowledge load` 返回的 CONTEXT 术语，并遵守其中已加载的 RULE；不要自行定位或重复读取领域文档。
