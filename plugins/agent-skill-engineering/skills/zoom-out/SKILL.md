---
name: zoom-out
description: 让 agent 跳出当前代码，给出更高层次的全局视角。当你对某段代码不熟悉，或需要理解它在整体中的位置时使用。
---

我对这块代码不熟悉。请上升一层抽象。给我一张所有相关模块和调用者的地图，使用项目领域术语表（`CONTEXT.md`）中的词汇；所有项目规则均必须遵守，不要给出与任何 RULES 冲突的结构描述。领域文档按 `docs/agents/domain.md` 的布局定位。
