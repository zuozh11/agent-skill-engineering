---
name: impl
description: 按任务卡实现代码变更。读取 task 文件，加载领域知识和编码约束，实现变更并原子提交。适用于用户指定一个 task 文件要求实现，或说「实现 T1」「开发这个任务」「impl」「实现」等场景。
---

# Impl

按任务卡实现代码变更的通用 skill。一个 task = 一次实现 = 一个原子提交。

## 流程

### 1. 读取任务

读取用户指定的 task 文件（`docs/scratch/<feature>/tasks/NN-xxx.md`）。理解：
- What to build（端到端行为）
- 改动文件（路径 + 现状）
- 改动内容（伪码 / SQL / 字段清单）
- 验收条件

如果用户没有指定文件而是口头描述任务，按描述执行，但仍遵循下面的约束加载流程。

### 2. 加载领域知识与编码约束

按需加载，不要一次性全读：

| 需要了解      | 去哪里看                    |
|-----------|-------------------------|
| 业务术语、实体关系 | `CONTEXT.md`            |
| 架构硬约束     | `docs/adr/`（只读与当前改动相关的） |

### 3. 实现

- 先查看任务涉及文件的现有代码（grep 同类实现、读取目标文件）
- 使用 CONTEXT.md 中的术语命名变量和方法
- 遵守 ADR 中的约束
- 遵守 CLAUDE.md 中的规则
- 不添加任务范围之外的功能、重构或抽象

### 4. 验证

- 对照任务验收条件逐条自检
- 如果验证失败，修复后重新验证

### 5. 原子提交

一个 task = 一个 commit。

实现完成后，将 task 文件顶部的 `Status:` 改为 `completed`。

## 硬性规则

- **不扩展 scope**：只做任务卡描述的改动，发现相邻问题只提醒不修
- **不过度抽象**：匹配现有代码风格，不引入任务未要求的设计模式
- **不猜 API**：不确定的框架 API 先 grep 现有代码中的用法，再查 references
