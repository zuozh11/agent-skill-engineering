---
name: zoom-out
description: 让 agent 跳出当前代码，给出更高层次的全局视角。当你对某段代码不熟悉，或需要理解它在整体中的位置时使用。
---

# Zoom Out

上升一层抽象，给出所有相关模块和调用者的地图。使用当前任务已经由 `project-knowledge load` 返回的 CONTEXT 术语与 RULE，不自行定位或再读领域文档。项目未采用知识结构或 Hook 已提醒知识不可用时，继续执行并如实说明。
