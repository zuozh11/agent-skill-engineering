---
name: code-review
description: 只读评审固定代码范围；Standards 逐条核对项目规范 RULE 与代码必要性，Spec 只核对 PRD、任务卡等来源中的业务规则。适用于评审分支、PR、未提交改动、文件、目录或当前实现；可用 --std 或 --spec 锁定维度。
---

# Code Review

对固定代码范围执行一次只读评审：

- **Standards**：逐条检查项目全部 `RULE`、Agent 工程指令与代码必要性。
- **Spec**：只检查 PRD、API 清单、任务卡、Issue 和当前对话中已确认的业务规则。

调用方或用户指定 `--std` / `--spec` 时，视为维度锁：只跑被锁定的维度。未锁维度时分别执行两个有依据的维度。依据归属固定：项目 `RULE` 只进 Standards，业务规则只进 Spec；两个维度只共享固定代码范围，不共享依据或 finding。本 Skill 不修改代码、提交或远端状态。

## 1. 加载项目上下文

按项目知识协议使用相关 CONTEXT。启用 Standards 时先稳定枚举项目提供的全部 RULE 文件，记录每个 RULE 的原始标题、路径和顺序，得到 RULE 清单与总数 `R`。随后从头到尾读取清单中的每个文件；索引、摘要、搜索命中、历史记忆和只读标题都不能代替正文。输出被截断时分段继续读取到 EOF。范围扩大时重新生成清单并全文读取。

本 Skill 中，项目 `RULE` 指项目知识协议加载的规范文件，全部归入 Standards；即使某条 `RULE` 提到状态、数据、校验、权限或业务流程，也仍是项目规范，不进入 Spec。业务规则指 PRD、API 清单、任务卡、Issue 和当前对话中的已确认要求，只归入 Spec，不进入 Standards。历史或被当前明确要求取代的材料不作为评审依据。

任一 RULE 无法枚举、打开或读完时，Standards 尚未开始。只报告读取失败的路径或缺口，不输出部分评审结果。

## 2. 固定评审范围

优先使用调用方或用户指定的 commit、branch、tag、range、PR 或路径，并记录实际 diff 命令和 commit 列表。分支使用 `git diff <fixed-point>...HEAD`。

调用方指定的 commit、range 或 pathspec 与用户指定同等优先。`impl` 传入单笔提交时，范围就是该 SHA，不使用分支 merge-base。

用户未明确固定点时：

- PR 编号或 URL：解析真实 base、head 和 patch；
- 当前分支：使用可确认的上游或默认分支 merge-base，无法确认时提问；
- 未提交改动：覆盖未暂存、已暂存和未跟踪文件；
- 文件或目录当前实现：完整读取 [SNAPSHOT.md](./SNAPSHOT.md) 固定文件列表和工作区状态。

用户指定路径时用同一 pathspec 限制范围。引用无效、diff 为空或 snapshot 无有效文件时停止。

## 3. Standards 评审

读取范围内生效的 `AGENTS.md`、`CLAUDE.md`、项目完整 RULE 集合，以及用户明确指定的其他工程规范。PRD、任务卡等需求不进入 Standards。记录每个来源的路径、准确标题和具体条款；没有明确来源的观点不是评审依据。

调用方给出的候选 `ruleId` 全部进入 Standards 的 RULE 清单，但候选列表不能替代或缩小项目完整 RULE 集合。

先建立完整评审范围清单：记录全部文件及 changed hunk；snapshot 模式记录全部目标文件及其中需要审查的代码段。得到代码段总数 `H`。范围较大时可以稳定分批，但每个批次都属于同一次评审，不能只审“重点路径”或抽样。

### RULE 逐条核对

按照 RULE 清单顺序逐个处理。对一个 RULE，先把该 RULE 的完整正文与完整评审范围逐段对照，完成后才处理下一个 RULE。不按关键词、文件名或主观相关性预筛。每个 RULE 的原始标题在最终表中必须且只能出现一次，并判定为：

- `违反`：固定范围内有代码直接违反具体条款；
- `符合`：至少一个条款适用于固定范围，完整核对后没有发现违反；
- `不适用`：整个 RULE 约束的技术、模块或场景都未出现在固定范围，原因必须可复核。

只要一个适用条款违反，整条 RULE 判为 `违反`，并在同一行列全该 RULE 的所有违反证据。存在适用条款时不能判为 `不适用`。`违反`必须同时给出 RULE 的路径与具体条款、代码位置、违反原因和大致修改方向；不能指出具体条款或代码证据时，继续核实，不能生成问题。

### 必要性审查

完整读取 [MIN-IMPL.md](../codebase-design/MIN-IMPL.md)。对范围清单中的每个 changed hunk 及其完整 enclosing symbol 逐段回答：

1. 这段代码具体做什么，服务哪个已确认行为或真实调用者？
2. 它必须存在吗；删除后，已确认行为是否仍然成立？
3. 删除、复用现有代码、标准库、平台原生能力或已安装依赖，能否让实现更简单？
4. 删除或替换后，文件、类型、分支、状态和概念的净数量是减少还是增加？

如果删除后行为仍正确且实现更简单，这段代码违反 `MIN-IMPL.md`，应当删除。如果必须用更复杂的实现才能维持正确行为，则保留。只有能够说明真实调用路径、删除前后行为和净复杂度变化时，才能报告必要性问题；猜测未来扩展、泛泛代码味道、个人偏好和“也许更优雅”都不构成问题来源。

修复缺陷时检查待修改位置的全部调用者；共同根因能够在真实所有者修复时，把散落在调用者的补丁作为 `MIN-IMPL.md` 违反报告。`codebase-design` 只在已证明确有必要调整模块形状时读取。

每审完一个代码段就在内部范围清单标记一次；必须达到 `H/H`。必要性问题统一归入表中的 `最小实现阶梯 / MIN-IMPL.md` 行。任何问题缺少明确来源或具体证据时删除该问题，不影响继续完成剩余 RULE 和代码段。

## 4. Spec 业务规则评审

按以下顺序读取本次需求依据：

1. 用户指定的 PRD、API 清单、任务卡、Issue、PR 或当前对话目标；
2. commit 或变更明确引用的需求来源；
3. 与变更主题明确对应的本地需求文档。

CONTEXT 可帮助理解项目术语，但不代替需求来源。Spec 不读取项目 `RULE`，也不把当前代码或工程惯例转成业务规则。没有可用业务规则依据时跳过 Spec，并如实说明。

Spec 只报告需求遗漏、错误行为和需求之外的 scope creep；命名、重复、架构和测试写法不进入 Spec。每条适用的已确认需求都在本轮内部判定为符合、不适用或违反。

调用方给出的需求材料必须纳入 Spec 分类，不得静默丢弃。需求判定不适用只允许四类可复核理由：历史、场景不匹配、被本次明确要求取代、不在本单元业务结果内；缺这类理由时保持适用。调用方声明本单元业务结果后，未纳入该结果的其余需求判不适用，不报遗漏。

需求材料在本提交中已被用户确认修正时，以修正后的文本为 Spec 依据。实现方私下的解释、注释或偏差记录不构成依据。Implementation Decisions 中来源为用户确认的授权例外，对应条款判定为不适用，并在覆盖状态中说明。

## 5. 执行与终检

两个维度使用隔离输入：

- Standards 只接收固定代码范围、项目 RULE、Agent 工程指令和 `MIN-IMPL.md`；不接收 PRD、API 清单、任务卡、Issue、对话业务要求或 Spec 候选问题。
- Spec 只接收固定代码范围和已确认业务规则来源；不接收项目 RULE、Agent 工程指令、`MIN-IMPL.md`、Standards 表或 Standards 候选问题，也不执行 RULE 枚举、读取、判定或覆盖统计。

只启用 Spec 时可由一个 Spec 子 Agent 完成；两个维度同时启用时并行执行，完成前不交换中间结论。Standards 可由主 Agent 完成，也可按 RULE 清单的连续区间分批委派；每个批次都接收完整固定范围，返回其区间内每个 RULE 的唯一判定。分批只改变执行方式，不改变完整覆盖要求。

主 Agent 维护并终检两份账本：RULE 清单 `R` 条和代码段清单 `H` 段。输出前必须同时满足：

- Standards 表中的项目 RULE 行数等于 `R`；
- 表中 RULE 标题集合与清单完全相等，顺序一致、没有重复；
- 每行判定只能是 `符合`、`违反`、`不适用`；
- 代码段清单全部完成，达到 `H/H`；
- 每个违反都能定位到具体 RULE 条款和代码证据。

任一条件未满足时继续补齐。因外部缺口确实无法继续时，只输出 `Standards 评审未完成`、已完成数量和具体缺口；不得输出违反清单、通过结论或任何部分 RULE 表。

保留问题前必须同时确认：位置准确、影响具体、依据属于本维度、建议是最小正确方向。Spec finding 的严重程度由终检结果确定：`P0` 阻断交付或造成重大事故，`P1` 高概率错误行为，`P2` 局部缺陷或明确维护风险，`P3` 低风险但有具体收益。

## 6. 输出

`--std` 只输出评审范围和一张 Standards 表。未锁维度或 `--spec` 时，只为启用的维度追加对应内容。

Standards 表必须按清单顺序包含项目全部 RULE 的原始标题，每个标题恰好一行。一个 RULE 有多个违反时全部写在同一行。其他工程规范和 `最小实现阶梯 / MIN-IMPL.md` 各自最多增加一行；没有明确来源的问题不输出。Standards 的任何结果都写在这张表中，不在表后追加“补充核实”、遗漏项、额外 finding 或部分覆盖声明。

```markdown
评审范围：<固定点、diff 命令和路径>

## Standards

| Rule 标题 | 判定 | 源头 | 代码证据 | 原因 | 大致修改方向 |
|---|---|---|---|---|---|
| `<原始标题>` | `符合 / 违反 / 不适用` | `<来源路径:行，具体条款>` | `<全部违反的代码路径:行；无则 —>` | `<判定理由>` | `<违反时写最小方向；否则 —>` |
| 最小实现阶梯 / MIN-IMPL.md | `符合 / 违反 / 不适用` | `codebase-design/MIN-IMPL.md:<行，具体条款>` | `<全部违反的代码路径:行；无则 —>` | `<用途、真实调用者、删除前后行为及净复杂度变化>` | `<违反时写删除或更简单的替代；否则 —>` |

## Spec
- [SPEC-001] [P1] <问题> — `<文件:行>`
  - 需求依据：<需求来源及具体要求>
  - 偏差：<遗漏、错误或 scope creep>
  - 最小修复：<方向>

需求：已覆盖 <来源列表>；<通过 / 存在上述违反 / 未完成>
```

表后只写一行结论：`Standards：已完成 R/R 条 RULE、H/H 个代码段；通过 / 存在 N 条违反`。不得用“重点路径已审”“其余需补充”“主审后续合并”等措辞结束。没有违反时也必须保留完整 RULE 表，不制造改进意见，不估算净减少行数。
