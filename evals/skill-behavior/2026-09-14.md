# 2026-09-14 行为验证记录

基线为 `b3e377b` 加本轮开始时已有的 `code-review/SKILL.md` 修改；候选为本轮技能优化。每个样例使用独立临时仓库和无会话继承的子 Agent，模型与推理设置继承同一父会话，未显式覆盖。执行者只收到任务、技能入口和原始样例；评估者再根据 `cases.json` 检查产物与执行报告。

| 样例 | 结果 | 观察 |
| --- | --- | --- |
| `prd-complete` | 通过 | 原版与新版都完成 PRD 且没有重复提问；原版加载 `ask-me` 后零轮返回，新版直接成稿。 |
| `prd-open` | 通过 | 写出不依赖退款方式的草稿，仅询问立即退款或审批退款，未把推荐项写成锁定决策。 |
| `knowledge-authorized` | 通过 | 直接写入已授权的日志规则及命令行例外；只改 A01，其他规则哈希不变，验证通过，无重复确认。 |
| `spec-only` | 通过 | 找到会员折扣遗漏；只读 `SPEC.md` 与 snapshot 参考，未读 RULE、Standards 或最小实现阶梯；工作区不变。 |
| `standards-all` | 通过 | 完整读取三条 RULE，分别判定违反、符合、不适用；完成 3/3 RULE、2/2 代码段；未读取 PRD 或 Spec，工作区不变。 |
| `repair-complete` | 通过 | 复现原始失败，完成修复、独立双维度评审、补充回归与 amend；最终三项测试通过，入口输出 900，一笔本地提交且工作区干净，无推送或重复提问。 |

首轮修复样例的执行者被要求不得继续委派，因而仅验证交付完成，不能据此证明双维度隔离。单独的 Standards 与 Spec 样例验证了各自的读取边界。

随后在新工作区重跑完整修复流程，允许按技能委派：Standards 与 Spec 分别在无历史继承的上下文中评审同一固定提交，未接收实现方结论。Standards 完成 3/3 RULE、1/1 代码段；Spec 找到现有两项测试未覆盖的大额安全整数精度问题。执行者先补充失败回归，修复后将测试与实现 amend 到同一提交，最终三项测试通过，并用 BigInt 精确计算交叉验证 20,002 个金额；没有重复评审或用户确认。

原版和候选初版的多个样例误在单 Context 项目中传入 `--context`。根据这个实际失败，在协议中补充单 Context 自动加载、多 Context 才使用该参数；随后用全新上下文重跑完整 PRD，直接执行 `load --rule C` 成功，没有报错重试。

文本体积采用字符数，不是 token 或运行成本：

| 入口 | 原版 | 候选 |
| --- | ---: | ---: |
| 全部技能 description（含字段名前缀） | 986 | 701 |
| `code-review/SKILL.md` | 4900 | 1442 |
| `to-api/SKILL.md` | 3417 | 1726 |
| `setup-agent-skills/SKILL.md` | 5607 | 4992 |
| 普通消息 Hook 正文 | 507 | 107 |

技能根文件缩短包含内容移到参考文件的效果，不能当作所有执行分支都减少同样的加载量。Standards 全文覆盖保证未删减，恢复事件仍携带完整协议。

自动验证：知识脚本 27 项和镜像 2 项测试通过；12 个技能的 frontmatter 校验、插件 manifest 校验、非模板指令链接与 Git 空白检查通过。没有多模型、重复随机运行、token 计量或耗时对照；本记录是行为冒烟结果，不代表统计成功率或跨模型性能结论。
